services:
  chat:
    build: ./server
    container_name: portfolio-chat
    environment:
      - NODE_ENV=production
      - PORT=3001
      - CORS_ORIGIN=*
    ports:
      - "13013:3001"
    restart: unless-stopped
